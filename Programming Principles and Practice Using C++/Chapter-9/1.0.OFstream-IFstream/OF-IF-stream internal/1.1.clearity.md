இந்த code-ஐ run பண்ணினால் `open()`, `write()`, `read()`, `close()` என்ன return பண்ணுகிறது என்று live-ஆ பார்க்கலாம்.

```cpp
#include <iostream>
#include <fcntl.h>
#include <unistd.h>
#include <cstring>

int main()
{
    std::cout
        << "====================================\n";

    std::cout
        << "STEP 1 : OPEN FILE\n";

    std::cout
        << "====================================\n";

    int fd = open(
        "person.txt",
        O_WRONLY | O_CREAT | O_TRUNC,
        0644
    );

    std::cout
        << "open() return value (fd) : "
        << fd
        << '\n';

    std::cout
        << "\n====================================\n";

    std::cout
        << "STEP 2 : WRITE FILE\n";

    std::cout
        << "====================================\n";

    const char* text =
        "Tony Stark";

    ssize_t bytes_written =
        write(
            fd,
            text,
            strlen(text)
        );

    std::cout
        << "text                 : "
        << text
        << '\n';

    std::cout
        << "strlen(text)         : "
        << strlen(text)
        << '\n';

    std::cout
        << "write() return value : "
        << bytes_written
        << '\n';

    std::cout
        << "\n====================================\n";

    std::cout
        << "STEP 3 : CLOSE FILE\n";

    std::cout
        << "====================================\n";

    int close_result =
        close(fd);

    std::cout
        << "close() return value : "
        << close_result
        << '\n';

    std::cout
        << "\n====================================\n";

    std::cout
        << "STEP 4 : OPEN AGAIN FOR READ\n";

    std::cout
        << "====================================\n";

    fd = open(
        "person.txt",
        O_RDONLY
    );

    std::cout
        << "open() return value (fd) : "
        << fd
        << '\n';

    std::cout
        << "\n====================================\n";

    std::cout
        << "STEP 5 : READ FILE\n";

    std::cout
        << "====================================\n";

    char buffer[100];

    ssize_t bytes_read =
        read(
            fd,
            buffer,
            sizeof(buffer) - 1
        );

    buffer[bytes_read] = '\0';

    std::cout
        << "read() return value : "
        << bytes_read
        << '\n';

    std::cout
        << "buffer content      : "
        << buffer
        << '\n';

    std::cout
        << "\n====================================\n";

    std::cout
        << "STEP 6 : CLOSE FILE\n";

    std::cout
        << "====================================\n";

    close_result =
        close(fd);

    std::cout
        << "close() return value : "
        << close_result
        << '\n';

    return 0;
}
```

---

Typical output:

```text
====================================
STEP 1 : OPEN FILE
====================================
open() return value (fd) : 3

====================================
STEP 2 : WRITE FILE
====================================
text                 : Tony Stark
strlen(text)         : 10
write() return value : 10

====================================
STEP 3 : CLOSE FILE
====================================
close() return value : 0

====================================
STEP 4 : OPEN AGAIN FOR READ
====================================
open() return value (fd) : 3

====================================
STEP 5 : READ FILE
====================================
read() return value : 10
buffer content      : Tony Stark

====================================
STEP 6 : CLOSE FILE
====================================
close() return value : 0
```

இதில் கவனிக்க:

```text
open()  -> 3
```

இது:

```text
Kernel:
FD 3 = person.txt
```

---

```text
write() -> 10
```

இது:

```text
Tony Stark
```

என்ற 10 bytes successfully எழுதப்பட்டன.

---

```text
read() -> 10
```

இது:

```text
10 bytes buffer-க்குள் copy செய்யப்பட்டன
```

என்று அர்த்தம்.

---

```text
close() -> 0
```

இது:

```text
Success
```

என்று அர்த்தம்.

🔥 இந்த program-ஐ `g++ file.cpp && ./a.out` run பண்ணினால் OS file descriptor system எப்படி வேலை செய்கிறது என்பது தெளிவாக புரியும்.
