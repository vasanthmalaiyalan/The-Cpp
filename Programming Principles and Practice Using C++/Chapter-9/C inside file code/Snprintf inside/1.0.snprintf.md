`snprintf()`-ன் real implementation மிகவும் பெரியது. ஏனெனில் அது:

```text
%d
%u
%x
%o
%f
%lf
%c
%s
%p
width
precision
alignment
locale
...
```

எல்லாவற்றையும் handle செய்கிறது.

ஆனால் `%d` மட்டும் support செய்யும் simplified `snprintf()`-ஐ எழுதினால் அதன் core idea தெளிவாக புரியும்.

---

# Simple snprintf clone

```c
#include <stdio.h>

int my_snprintf(
    char* buffer,
    int buffer_size,
    const char* format,
    int value
)
{
    int temp = value;

    char digits[32];

    int digit_count = 0;

    // ------------------------------------
    // integer -> string
    // ------------------------------------

    do
    {
        digits[digit_count] =
            (temp % 10) + '0';

        temp /= 10;

        digit_count++;

    } while (temp > 0);

    // ------------------------------------
    // build output
    // ------------------------------------

    int pos = 0;

    while (
        *format &&
        pos < buffer_size - 1
    )
    {
        if (
            format[0] == '%' &&
            format[1] == 'd'
        )
        {
            int i;

            for (
                i = digit_count - 1;
                i >= 0;
                i--
            )
            {
                if (
                    pos >= buffer_size - 1
                )
                {
                    break;
                }

                buffer[pos++] =
                    digits[i];
            }

            format += 2;
        }
        else
        {
            buffer[pos++] =
                *format;

            format++;
        }
    }

    buffer[pos] = '\0';

    return pos;
}

int main()
{
    char buffer[100];

    my_snprintf(
        buffer,
        sizeof(buffer),
        "Age = %d",
        45
    );

    printf("%s\n", buffer);
}
```

Output:

```text
Age = 45
```

---

# Internally Step By Step

Call:

```c
my_snprintf(
    buffer,
    100,
    "Age = %d",
    45
);
```

---

Format string memory:

```text
A g e _ = _ % d \0
```

---

Loop:

```text
copy A
copy g
copy e
copy space
copy =
copy space
```

Buffer:

```text
Age =
```

---

Then sees:

```text
%d
```

---

Convert:

```text
45
```

to chars:

```text
'4'
'5'
```

---

Append:

```text
Age = 45
```

---

Terminate:

```text
\0
```

Final memory:

```text
A g e _ = _ 4 5 \0
```

---

Real `snprintf()` conceptually does:

```c
int snprintf(
    char* buffer,
    size_t size,
    const char* format,
    ...
)
{
    parse_format_string();

    convert_arguments();

    write_to_buffer();

    prevent_overflow();

    add_null_terminator();
}
```

---

ஒரு interesting thing:

```c
snprintf(
    buffer,
    5,
    "Tony Stark"
);
```

Output:

```text
Tony
```

ஏனெனில்:

```text
buffer_size = 5

T
o
n
y
\0
```

மட்டுமே fit ஆகும்.

அதனால்தான் `snprintf()` என்பது:

```text
sprintf()
+
buffer overflow protection
```

என்று சொல்லலாம். 🔥

-------------------------------


``{
            buffer[pos++] =
                *format;

            format++;
        }` எப்படி இந்த convert நடக்கும் `Format string memory:
Plain text
A g e _ = _ % d \0
Loop:
Plain text
copy A
copy g
copy e
copy space
copy =
copy space
Buffer:
Plain text
Age =
Then sees:
Plain text
%d
Convert:
Plain text
45` இதுவும் char ஆகத்தானே போகும் like '%' 'd' ?`


ஆமாம்! 🔥 நீ சரியான இடத்தில் கேள்வி கேட்கிறாய்.

முதலில் இது:

```c
buffer[pos++] = *format;
format++;
```

என்ன செய்கிறது?

---

Suppose:

```c
const char* format = "Age = %d";
```

Memory:

```text
Address    Value

1000       'A'
1001       'g'
1002       'e'
1003       ' '
1004       '='
1005       ' '
1006       '%'
1007       'd'
1008       '\0'
```

---

Initially:

```text
format = 1000
```

அதனால்:

```c
*format
```

means:

```text
'A'
```

---

Execute:

```c
buffer[pos++] = *format;
```

Equivalent:

```c
buffer[0] = 'A';
```

---

Then:

```c
format++;
```

Now:

```text
format = 1001
```

---

Next iteration:

```c
*format
```

becomes:

```text
'g'
```

Copy:

```c
buffer[1] = 'g';
```

---

அப்படியே:

```text
A
g
e
space
=
space
```

copy ஆகும்.

---

இப்போது முக்கியமான பகுதி 🔥

Pointer reaches:

```text
1006 -> '%'
1007 -> 'd'
```

---

அப்போது code:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

check செய்கிறது.

---

இங்கே:

```c
format[0]
```

means:

```text
'%'
```

---

and:

```c
format[1]
```

means:

```text
'd'
```

---

Condition true.

---

இப்போது important realization:

இந்த `%` மற்றும் `d`

```text
buffer-க்கு copy ஆகாது
```

❌

---

அதற்கு பதிலாக program இதை:

```text
special instruction
```

என்று treat செய்கிறது.

---

அதாவது:

```text
%d
```

means:

```text
"next integer argument-ஐ string-ஆ convert பண்ணு"
```

---

Example:

```c
my_snprintf(
    buffer,
    100,
    "Age = %d",
    45
);
```

---

Arguments:

```text
format = "Age = %d"

value = 45
```

---

Program sees:

```text
%d
```

and says:

```text
don't copy '%' and 'd'

instead use value
```

---

Then:

```text
45
```

convert ஆகும்:

```text
'4'
'5'
```

---

Buffer becomes:

```text
A
g
e
space
=
space
4
5
\0
```

---

Final string:

```text
Age = 45
```

---

இதையே இன்னும் simple-ஆ:

Suppose:

```c
printf("Hello %d", 99);
```

---

Memory:

```text
H e l l o _ % d
```

---

Compiler:

```text
does NOT replace %d
```

---

Runtime:

```text
printf()
```

function தான் parse செய்கிறது.

---

Runtime sees:

```text
%d
```

and interprets it as:

```text
placeholder
```

not normal text.

---

அதனால்:

| Character               | Action                     |
| ----------------------- | -------------------------- |
| `'A'`                   | copy                       |
| `'g'`                   | copy                       |
| `'='`                   | copy                       |
| `'%'` followed by `'d'` | special format instruction |
| integer 45              | convert to `'4'`, `'5'`    |

---

அதனால்தான்:

```c
printf("%d", 45);
```

output:

```text
45
```

ஆகிறது.

இல்லையென்றால் output:

```text
%d
```

ஆகவே வந்திருக்கும் 😄

இதுதான் format string parser-ன் அடிப்படை idea. `printf`, `fprintf`, `sprintf`, `snprintf`, `std::format` எல்லாமே இதே concept-ஐ பெரிய அளவில் implement செய்கின்றன. 🔥


---------------------------

`சரி புரிந்தது `if (
    format[0] == '%' &&
    format[1] == 'd'
)`இதை எதற்கு தேவையில்லாமல் கொடுத்துள்ளாய் ?`


நல்ல observation 🔥

உண்மையில் அந்த code-ல்:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

இல்லையென்றால் `%d`-ஐ கண்டுபிடிக்க முடியாது.

பார்:

```c
my_snprintf(
    buffer,
    100,
    "Age = %d",
    45
);
```

Memory:

```text
A g e _ = _ % d \0
```

Loop simply:

```c
buffer[pos++] = *format;
format++;
```

மட்டும் செய்தால்:

```text
A
g
e
space
=
space
%
d
```

எல்லாமே copy ஆகிவிடும்.

Result:

```text
Age = %d
```

ஆகும்.

---

ஆனால் நமக்கு வேண்டும்:

```text
Age = 45
```

---

அதனால் parser ஒரு இடத்தில்:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

என்று check பண்ண வேண்டும்.

அது essentially:

```text
"இங்கே ஒரு special instruction இருக்கு"
```

என்று detect பண்ணுகிறது.

---

Real `printf()`-ல் இதைவிட பெரிய parser இருக்கும்.

Roughly:

```c
if (*format == '%')
{
    format++;

    switch(*format)
    {
        case 'd':
            // integer
            break;

        case 's':
            // string
            break;

        case 'c':
            // char
            break;

        case 'f':
            // float
            break;
    }
}
```

---

உதாரணம்:

```c
printf(
    "Name=%s Age=%d",
    "Tony",
    45
);
```

Memory:

```text
N a m e = % s
A g e = % d
```

Parser:

```text
%s → string argument
%d → integer argument
```

என்று decide பண்ணும்.

---

அதனால்:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

நான் கொடுத்தது ஒரு **minimal format parser**.

அதன் வேலை:

```text
ordinary characters
        ↓
copy

%d
        ↓
convert integer
```

என்று branch செய்ய.

---

அந்த `if` இல்லையென்றால்:

```text
%d
```

என்பது ஒரு normal text மாதிரி treat ஆகும்.

அப்போ output:

```text
Age = %d
```

வரும்.

---

அதனால் அந்த check-ன் purpose:

```text
"இது சாதாரண character இல்லை,
format specifier"
```

என்று identify செய்வது. 🔥

----------------------------

``if (
    format[0] == '%' &&
    format[1] == 'd'
)`ஆனால் இது format[0] என்பது 'A' தானே குறிக்கும்?`


🔥 சரியாகப் பிடித்துவிட்டாய்!

ஆமாம், loop ஆரம்பிக்கும் போது:

```c
const char* format = "Age = %d";
```

என்றால்:

```text
format
  ↓
'A'
```

ஆகவே ஆரம்பத்தில்:

```c
format[0]
```

உண்மையில்:

```text
'A'
```

தான்.

---

அதனால் முதல் iteration:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

check செய்தால்:

```c
'A' == '%'
```

false.

---

அதனால்:

```c
buffer[pos++] = *format;
format++;
```

execute ஆகும்.

---

இப்போது:

```text
format
  ↓
'g'
```

---

மீண்டும்:

```c
format[0]
```

=

```text
'g'
```

---

Again:

```c
'g' == '%'
```

false.

---

இப்படியே:

```text
A
g
e
space
=
space
```

எல்லாம் copy ஆகும்.

---

பிறகு pointer இங்கே வரும்:

```text
A g e _ = _ % d \0
            ↑
          format
```

இப்போது:

```c
format[0]
```

=

```text
'%'
```

---

and

```c
format[1]
```

=

```text
'd'
```

---

இப்போது condition:

```c
format[0] == '%'
```

✅ true

and

```c
format[1] == 'd'
```

✅ true

---

அதனால் இப்போதுதான்:

```c
if (
    format[0] == '%' &&
    format[1] == 'd'
)
```

block execute ஆகும்.

---

Visual trace:

```text
Iteration 1

format -> "Age = %d"
           ↑

format[0] = 'A'

false
```

---

```text
Iteration 2

format -> "ge = %d"
           ↑

format[0] = 'g'

false
```

---

```text
Iteration 3

format -> "e = %d"
           ↑

format[0] = 'e'

false
```

---

```text
Iteration 4

format -> " = %d"
           ↑

format[0] = ' '

false
```

---

```text
Iteration 5

format -> "= %d"
           ↑

format[0] = '='

false
```

---

```text
Iteration 6

format -> " %d"
           ↑

format[0] = ' '

false
```

---

```text
Iteration 7

format -> "%d"
           ↑

format[0] = '%'
format[1] = 'd'

TRUE
```

🔥

இதுதான் trick.

`format` என்பது எப்போதும் string-ன் beginning-ஐ point செய்யவில்லை.

நீ:

```c
format++;
```

செய்யும் ஒவ்வொரு முறையும் pointer next character-க்கு move ஆகிறது.

அதனால்:

```c
format[0]
```

என்பது:

```text
string beginning
```

இல்லை.

அது:

```text
pointer தற்போது எந்த character-ஐ point செய்கிறதோ
அந்த character
```

என்பதுதான்.

இதுதான் pointer arithmetic-ன் beauty:

```c
format[0]
```

and

```c
*format
```

இரண்டும் equivalent.

```c
format[1]
```

means:

```c
*(format + 1)
```

அதாவது current position-லிருந்து next character. 🔥


---------------------------------


`எனக்கு புரியல இது எப்படி சாத்தியம் `இப்போது:
Plain text
format
  ↓
'g'
மீண்டும்:
C
format[0]
=
Plain text
'g'` if move A to g means like `format[1]`இப்படித்தான் இருந்திருக்க வேண்டும்?`



இதுதான் pointer-ஐ புரிந்துகொள்ளும் முக்கியமான தருணம் 🔥

நீ இப்படி நினைக்கிறாய்:

```text
format[0] = A
format[1] = g
format[2] = e
...
```

இது ஆரம்பத்தில் மட்டும் சரி.

---

முதலில்:

```c
const char* format = "Age = %d";
```

Memory:

```text
Address    Value

1000       'A'
1001       'g'
1002       'e'
1003       ' '
1004       '='
1005       ' '
1006       '%'
1007       'd'
1008       '\0'
```

Pointer:

```text
format = 1000
```

---

இப்போது:

```c
format[0]
```

Compiler internally:

```c
*(format + 0)
```

அதாவது:

```c
*(1000 + 0)
```

Result:

```text
'A'
```

---

இப்போது:

```c
format++;
```

நடக்கிறது.

Pointer value மாறுகிறது:

```text
format = 1001
```

---

இப்போது முக்கியமான விஷயம் 🔥

`format[0]` என்பது:

```c
*(format + 0)
```

தான்.

ஆனால் format இப்போது:

```text
1001
```

---

அதனால்:

```c
format[0]
```

becomes:

```c
*(1001 + 0)
```

Result:

```text
'g'
```

---

அதே நேரத்தில்:

```c
format[1]
```

becomes:

```c
*(1001 + 1)
```

Result:

```text
'e'
```

---

Visual:

### Before `format++`

```text
Address    Value

1000       'A'   <- format
1001       'g'
1002       'e'
```

So:

```text
format[0] = A
format[1] = g
format[2] = e
```

---

### After `format++`

```text
Address    Value

1000       'A'
1001       'g'   <- format
1002       'e'
1003       ' '
```

Now:

```text
format[0] = g
format[1] = e
format[2] = ' '
```

🔥🔥🔥

---

அதாவது:

```text
format[0]
```

என்பது:

```text
"original string-ன் first character"
```

இல்லை ❌

---

அது:

```text
"pointer தற்போது pointing செய்யும் character"
```

✅

---

ஒரு சிறிய code:

```c
#include <stdio.h>

int main()
{
    char text[] = "ABC";

    char* p = text;

    printf("%c\n", p[0]);

    p++;

    printf("%c\n", p[0]);
}
```

Output:

```text
A
B
```

---

ஏன்?

முதலில்:

```text
p -> A
```

பிறகு:

```text
p -> B
```

ஆகிவிட்டது.

`p[0]` என்ற expression மாறவில்லை.

ஆனால்:

```text
p
```

என்ற pointer value மாறிவிட்டது.

---

இதைத்தான்:

```c
arr[index]
```

=

```c
*(arr + index)
```

என்ற rule மூலம் புரிந்துகொள்ளலாம்.

`format[0]` என்பது fixed location அல்ல.

அது:

```c
*(format + 0)
```

அதனால் `format` move ஆனால் `format[0]` பார்க்கும் character-மும் மாறும். 🔥
