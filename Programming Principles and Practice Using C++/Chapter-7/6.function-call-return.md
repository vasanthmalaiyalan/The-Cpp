இந்த section function-ன் real purpose பற்றி பேச ஆரம்பிக்கிறது.

மிக முக்கியமான software engineering foundation இது.

---

# Core Idea

Author சொல்வது:

> “Functions are how we represent actions and computations.”

அதாவது:

Programming-ல்:
ஒரு meaningful வேலை செய்ய வேண்டுமென்றால்
நாம் function எழுதுவோம்.

---

# Example

```cpp id="jlwm0u"
int add(int a, int b)
{
    return a + b;
}
```

இந்த function:

* action செய்கிறது
* computation செய்கிறது

---

# Why functions important?

C++ already gives:

* operators
* statements

---

# Operators

```cpp id="4pn2bq"
+
-
*
/
```

இவை values உருவாக்க உதவும்.

---

# Example

```cpp id="s16pn0"
10 + 20
```

இதில்:

* operands = 10, 20
* operator = +

Result:

```text id="zyf4fk"
30
```

---

# Statements

Program execution control செய்ய.

---

# Examples

```cpp id="hl0wy7"
if
for
while
switch
```

---

# Example

```cpp id="3klh1m"
if (x > 0)
{
}
```

Decision making.

---

# But Problem

Large program:
operators + statements மட்டும் வைத்து build செய்ய முடியாது.

Code chaotic ஆகிவிடும்.

---

# Functions solve this

Functions:

* organize code
* divide work
* give names to actions
* improve readability

---

# Example Without Function

```cpp id="mjlwm5"
int total = price * quantity;

double tax = total * 0.18;

double final_price = total + tax;
```

பல இடங்களில் repeat ஆகும்.

---

# With Function

```cpp id="1mjlwm"
double calculate_price(double price, int quantity)
{
    double total = price * quantity;

    double tax = total * 0.18;

    return total + tax;
}
```

Much cleaner.

---

# Important Idea

Function = named behavior.

---

# Real-world analogy

Function என்பது:
ஒரு machine/tool போல.

---

# Example

Coffee machine:

Input:

* water
* coffee powder

Output:

* coffee

---

# Function

Input:

* arguments

Output:

* return value

---

# Function Arguments

Author says:

> “Functions usually need arguments.”

---

# Example

```cpp id="h2jlwm"
int add(int a, int b)
```

Arguments:

* a
* b

---

# Why needed?

Function work செய்ய data வேண்டும்.

---

# Function Return

Many functions:
result return செய்கின்றன.

---

# Example

```cpp id="jlwmcw"
return a + b;
```

Output:

```text id="7z2c77"
sum value
```

---

# Full Flow

```cpp id="uwp8p0"
int result = add(10, 20);
```

---

# What happens internally?

## Step 1

Arguments sent:

```text id="6vpfva"
10
20
```

---

## Step 2

Function executes:

```text id="lkjlwm"
10 + 20
```

---

## Step 3

Return value:

```text id="w2gw8y"
30
```

---

## Step 4

Stored in:

```text id="jlwmku"
result
```

---

# Functions are abstraction

மிக முக்கியமான concept.

When using:

```cpp id="0iiyvr"
sort(v.begin(), v.end());
```

நாம்:

* sorting algorithm details தெரிய வேண்டியதில்லை.

Function name போதும்.

---

# This is abstraction power

Complexity hidden.

---

# Functions organize primitives

Important sentence:

> “Operators and statements are primitives.”

அதாவது:
basic building blocks.

---

# Functions combine them

Example:

```cpp id="kjjlwm"
int square_sum(int a, int b)
{
    int x = a * a;

    int y = b * b;

    return x + y;
}
```

இதில்:

* operators
* variables
* return

all organized by function.

---

# Why real programs depend on functions

Without functions:

* huge repeated code
* impossible debugging
* unreadable logic

---

# Functions make code:

| Benefit      | Meaning           |
| ------------ | ----------------- |
| Reusable     | repeat usage      |
| Readable     | meaningful names  |
| Modular      | separated logic   |
| Testable     | isolated checking |
| Maintainable | easier updates    |

---

# BEST .CPP EXAMPLE

```cpp id="8jlwmv"
import std;


// =====================================
// Function declaration
// =====================================

double calculate_area(double radius);

void print_result(const std::string& label,
                  double value);


// =====================================
// Function definitions
// =====================================

double calculate_area(double radius)
{
    const double pi = 3.1415926535;

    return pi * radius * radius;
}


void print_result(const std::string& label,
                  double value)
{
    std::cout << label
              << " : "
              << value
              << '\n';
}


// =====================================
// Main
// =====================================

int main()
{
    // function arguments
    double radius = 5.0;


    // function call + return value
    double area = calculate_area(radius);


    // another function call
    print_result("Circle area", area);
}
```

---

# What this code teaches

| Concept              | Example                         |
| -------------------- | ------------------------------- |
| Function declaration | `double calculate_area(double)` |
| Arguments            | `radius`                        |
| Return value         | `return pi * radius * radius`   |
| Function call        | `calculate_area(radius)`        |
| Void function        | `print_result()`                |
| Code organization    | separated logic                 |

---

# Important Mental Model

## Function = Small Machine

| Part          | Meaning    |
| ------------- | ---------- |
| Arguments     | input      |
| Function body | processing |
| Return value  | output     |

---

# Real-world Professional Advice

Good programmers:

* split logic into functions
* keep functions small
* give meaningful names

---

# Most Important Lesson

Programming language primitives:

* operators
* loops
* if statements

alone not enough.

Functions are what turn:

* raw code

into:

* organized software systems.
