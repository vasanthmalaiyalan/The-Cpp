இந்த section மிகவும் important runtime concept பற்றி:

# Order of Evaluation

அதாவது:

> Program execute ஆகும் போது
> objects எப்போது உருவாகின்றன?
> எப்போது destroy ஆகின்றன?

---

# Core Idea

Program execution:
statement-by-statement நடக்கும்.

---

# Variable Definition Reached

When execution reaches:

```cpp id="4mwma0"
string s;
```

Two things happen:

---

# 1. Memory allocated

Object-க்கு memory reserve செய்யப்படும்.

---

# 2. Initialization

Object initialized.

For string:

```text id="7mwma1"
empty string ""
```

---

# This process called

# Construction

---

# Opposite Process

When variable scope முடிகிறது:

object destroyed.

---

# Called

# Destruction

---

# Very Important Lifecycle

```text id="9mwma2"
construct -> use -> destroy
```

---

# Book Example

---

# Global Variables

```cpp id="1mwma3"
string program_name = "silly";

vector<string> v;
```

---

# Important

Global variables:
before `main()`
start executing.

---

# Construction Order

Top-to-bottom.

---

# So:

```text id="5mwma4"
program_name constructed first
v constructed second
```

---

# Destruction Order

Reverse order.

---

# So:

```text id="2mwma5"
v destroyed first
program_name destroyed second
```

---

# Important Rule

## Construction:

top → bottom

## Destruction:

bottom → top

---

# Function Local Variables

```cpp id="8mwma6"
void f()
{
    string s;
}
```

---

# When f() called

`s`
constructed.

---

# When f() returns

`s`
destroyed.

---

# Lifetime

Only inside function call.

---

# While Loop Variables

```cpp id="6mwma7"
while (...)
{
    string stripped;
    string not_letters;
}
```

---

# Important

Each loop iteration:
new objects created.

---

# Example

Loop runs:

```text id="4mwma8"
10 times
```

Then:

* stripped constructed 10 times
* stripped destroyed 10 times

same for not_letters.

---

# Construction Order

```cpp id="9mwma9"
string stripped;
string not_letters;
```

---

# Constructed

```text id="0mwmb0"
stripped first
not_letters second
```

---

# Destroyed reverse order

```text id="7mwmb1"
not_letters first
stripped second
```

---

# Same Rule Everywhere

## Construction:

definition order

## Destruction:

reverse order

---

# Statement Scope Variable

```cpp id="3mwmb2"
for (char x : s)
```

---

# x exists only inside loop.

---

# Each iteration

`x`
constructed.

---

# End of iteration

`x`
destroyed.

---

# Important Point

```cpp id="1mwmb3"
x
```

does NOT exist after loop.

---

# Compiler Optimization

Very important real-world point.

---

# Author says

Compiler may optimize heavily.

---

# Example

Although:

```text id="5mwmb4"
stripped created/destroyed repeatedly
```

Compiler may reuse same memory.

---

# Why?

Only one `stripped`
alive at any time.

---

# So compiler cleverness

May internally allocate:

```text id="8mwmb5"
one reusable memory slot
```

---

# Important Professional Insight

What programmer WRITES
and what machine ACTUALLY DOES
can differ.

BUT:
observable behavior same.

---

# Stack Frame Connection

These local variables usually stored in:

# Stack frame

---

# Example

```cpp id="4mwmb6"
void f()
{
    string s;

    while (...)
    {
        string stripped;
    }
}
```

---

# Stack Conceptually

```text id="9mwmb7"
f stack frame
----------------
s
stripped memory
not_letters memory
```

---

# Memory reused repeatedly

Very efficient.

---

# Important Lifetime Concept

| Variable Type  | Lifetime             |
| -------------- | -------------------- |
| Global         | whole program        |
| Local          | function duration    |
| Block variable | block duration       |
| Loop variable  | loop iteration/block |

---

# Real-world Importance

This knowledge helps understand:

* memory usage
* object lifetime
* constructors/destructors
* RAII
* resource management
* stack behavior

---

# BEST BOOK-ORIENTED .CPP EXAMPLE

```cpp id="7mwmb8"
#include <iostream>
#include <string>
#include <vector>


// ======================================
// Global variables
// ======================================

std::string program_name = "demo";

std::vector<std::string> words;


// ======================================
// Function
// ======================================

void process()
{
    std::string s;


    while (std::cin >> s && s != "quit")
    {
        std::string stripped;

        std::string not_letters;


        for (char x : s)
        {
            if (std::isalpha(x))
            {
                stripped += x;
            }
            else
            {
                not_letters += x;
            }
        }


        words.push_back(stripped);


        std::cout
            << "stripped     : "
            << stripped
            << '\n';


        std::cout
            << "not_letters  : "
            << not_letters
            << '\n';
    }


    std::cout
        << "Leaving process()\n";
}


// ======================================
// Main
// ======================================

int main()
{
    std::cout
        << "Program started\n";


    process();


    std::cout
        << "Program ending\n";
}
```

---

# What this code teaches

| Concept                        | Example                           |
| ------------------------------ | --------------------------------- |
| Global lifetime                | `program_name`, `words`           |
| Local variable                 | `s`                               |
| Block scope variables          | `stripped`, `not_letters`         |
| Statement scope                | `char x`                          |
| Construction/destruction order | loop objects                      |
| Repeated object lifetime       | each loop iteration               |
| Scope-based lifetime           | variables disappear automatically |

---

# Most Important Lessons

## 1. Variables constructed when execution reaches definition

---

## 2. Variables destroyed when leaving scope

---

## 3. Globals live entire program

---

## 4. Local variables live during function call

---

## 5. Block variables recreated each iteration

---

## 6. Destruction happens reverse order of construction

---

## 7. Compiler may optimize memory reuse

---

# Real-world Professional Importance

Modern C++ heavily depends on:

* automatic object lifetime
* deterministic destruction
* scope-based resource management

This becomes the foundation for:

* RAII
* smart pointers
* exception safety
* memory management

One of the biggest strengths of C++.
