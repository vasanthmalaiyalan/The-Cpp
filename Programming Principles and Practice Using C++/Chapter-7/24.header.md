இந்த section traditional C++ modularity system பற்றி:

# Header Files

---

# Historical Context

Author சொல்வது:

Before modules,
C++ used:

# Header Files

for 50+ years.

---

# Why still important?

Because:

```text id="0twma0"
billions of lines of code
```

still use headers.

---

# Core Idea

Header file:
mainly contains:

# Declarations

---

# Example

```cpp id="5twma1"
class Token;

class Token_stream;

int f(int);
```

---

# Purpose

Other files can:

* know interfaces
* use functions/classes

without seeing implementations.

---

# Real-world Structure

Book gives:

```text id="9twma2"
token.h
token.cpp
```

---

# token.h

Contains:

## declarations

---

# token.cpp

Contains:

## definitions/implementations

---

# Very Important Software Engineering Pattern

# Separate Interface and Implementation

---

# Example

---

# token.h

```cpp id="3twma3"
class Token_stream
{
public:

    Token get();

    void putback(Token t);
};
```

---

# token.cpp

```cpp id="7twma4"
void Token_stream::putback(Token t)
{
    ...
}
```

---

# User only needs header

Compiler learns:

```text id="1twma5"
how to use
```

---

# Source file contains:

```text id="4twma6"
how it works internally
```

---

# #include

Important mechanism.

---

# Example

```cpp id="8twma7"
#include "f.h"
```

---

# Meaning

Conceptually:

```text id="2twma8"
Copy contents of f.h here
```

---

# Example

Header:

```cpp id="6twma9"
int f(int);
```

---

# user.cpp

```cpp id="0twmb0"
#include "f.h"

int g(int i)
{
    return f(i);
}
```

---

# Compiler Internally Sees

```cpp id="5twmb1"
int f(int);

int g(int i)
{
    return f(i);
}
```

---

# Important

This happens BEFORE compilation.

---

# Called

# Preprocessing

---

# Preprocessor

Handles:

```cpp id="9twmb2"
#include
#define
```

etc.

---

# Important Practice

Author says:

Include header BOTH:

* where declarations used
* where definitions implemented

---

# Why?

Compiler can verify consistency.

---

# Example Bug

Wrong implementation:

```cpp id="4twmb3"
Token Token_stream::putback(Token t)
{
    buffer.push_back(t);

    return t;
}
```

---

# Problems

Suppose actual declaration was:

```cpp id="8twmb4"
void putback(Token t);
```

---

# Compiler detects mismatch

Because:
header included.

---

# Errors Found

---

# Error 1

Return type mismatch:

```text id="3twmb5"
should return void
not Token
```

---

# Error 2

Suppose:

```cpp id="7twmb6"
buffer
```

is Token,
NOT vector<Token>.

Then:

```cpp id="1twmb7"
buffer.push_back()
```

invalid.

---

# Huge Benefit

Headers allow:
early error detection.

---

# Another Example

```cpp id="6twmb8"
ts.gett();
```

---

# Compiler immediately knows:

```text id="0twmb9"
gett() does not exist
```

because header provides declarations.

---

# Another Error

```cpp id="5twmc0"
ts.putback();
```

missing argument.

Compiler catches instantly.

---

# Important Professional Rule

Headers should contain ONLY things safe to duplicate.

---

# Why?

Header included in MANY files.

---

# Good Things in Headers

* function declarations
* class definitions
* constants
* templates

---

# Dangerous Things

Non-inline function definitions.

---

# Why dangerous?

Suppose:

```cpp id="9twmc1"
int f()
{
    return 1;
}
```

inside header.

Included in:
10 cpp files.

Now:
10 copies of f().

Linker chaos.

---

# Typical Modern Project Structure

```text id="4twmc2"
token.h
token.cpp
main.cpp
parser.cpp
```

---

# token.h

Interface.

---

# token.cpp

Implementation.

---

# main.cpp

Uses interface.

---

# Very Important Big Picture

Headers were:
old manual modularity mechanism.

Modules are:
modern language-level modularity.

---

# Header Weaknesses

Compared to modules:

* repeated parsing
* slow compilation
* macro pollution
* include-order problems

---

# But Still Everywhere

So every C++ programmer must understand headers deeply.

---

# BEST BOOK-ORIENTED EXAMPLE

# f.h

```cpp id="8twmc3"
#ifndef F_H
#define F_H


// ======================================
// Function declaration
// ======================================

int square(int x);


#endif
```

---

# f.cpp

```cpp id="3twmc4"
#include "f.h"


// ======================================
// Function definition
// ======================================

int square(int x)
{
    return x * x;
}
```

---

# main.cpp

```cpp id="7twmc5"
#include <iostream>

#include "f.h"


int main()
{
    int result =
        square(5);


    std::cout
        << "result = "
        << result
        << '\n';
}
```

---

# What this code teaches

| Concept              | Example                   |
| -------------------- | ------------------------- |
| Header file          | `f.h`                     |
| Declaration          | `int square(int)`         |
| Source file          | `f.cpp`                   |
| Definition           | `return x*x`              |
| Include mechanism    | `#include "f.h"`          |
| Preprocessing        | textual inclusion         |
| Interface separation | declaration vs definition |

---

# Important New Thing

```cpp id="1twmc6"
#ifndef F_H
#define F_H
...
#endif
```

called:

# Include Guard

---

# Why needed?

Prevents:
same header included multiple times.

Very important header technique.

---

# Most Important Lessons

## 1. Headers contain declarations/interfaces

---

## 2. Source files contain implementations

---

## 3. `#include` conceptually copies text

---

## 4. Preprocessor handles includes before compilation

---

## 5. Including headers improves consistency checking

---

## 6. Headers must avoid duplicate definitions

---

# Real-world Professional Importance

Understanding headers is essential for:

* legacy C++
* build systems
* large projects
* library development
* debugging compilation/linker errors

Even modern C++ developers still work extensively with headers today.
----------

`#ifndef`, `#define`, `#endif`

இவை C/C++ **preprocessor directives**.

Compiler compile செய்வதற்கு முன்னால்
preprocessor என்ற stage run ஆகும்.

அந்த stage-ல்:

* `#include`
* `#define`
* `#ifndef`
  போன்றவை process ஆகும்.

---

# Main Purpose

இந்த pattern:

```cpp id="0uwma0"
#ifndef F_H
#define F_H

// declarations

#endif
```

called:

# Include Guard

---

# Why Needed?

Suppose:

```text id="5uwma1"
main.cpp
```

includes:

```text id="9uwma2"
f.h
```

AND another header also includes `f.h`.

Then compiler same declarations-ஐ
multiple times பார்க்கும்.

---

# Example Problem

Without include guard:

```cpp id="3uwma3"
class Token
{
};
```

included twice.

Compiler sees:

```cpp id="7uwma4"
class Token {};
class Token {};
```

---

# Error

```text id="1uwma5"
redefinition of class Token
```

---

# Include Guard Solves This

---

# Step-by-step

## 1.

```cpp id="4uwma6"
#ifndef F_H
```

Means:

```text id="8uwma7"
"If F_H is NOT defined"
```

---

# 2.

```cpp id="2uwma8"
#define F_H
```

Means:

```text id="6uwma9"
"Now define F_H"
```

---

# 3.

Compiler processes header contents.

---

# 4.

```cpp id="0uwmb0"
#endif
```

Ends condition.

---

# First Include

Suppose first time:

```cpp id="5uwmb1"
#include "f.h"
```

---

# Preprocessor checks

```cpp id="9uwmb2"
#ifndef F_H
```

Is `F_H` defined?

NO.

So continue.

---

# Then

```cpp id="3uwmb3"
#define F_H
```

Now:

```text id="7uwmb4"
F_H exists
```

---

# Header contents included.

---

# Second Include

Again:

```cpp id="1uwmb5"
#include "f.h"
```

---

# Now:

```cpp id="4uwmb6"
#ifndef F_H
```

fails.

Because:

```text id="8uwmb7"
F_H already defined
```

---

# So compiler skips entire header.

Problem solved.

---

# Simple Analogy

```text id="2uwmb8"
"Have I already loaded this file?"
```

If yes:
skip.

---

# Important

`F_H`
is just a name.

Could also be:

```cpp id="6uwmb9"
TOKEN_H
MY_HEADER_H
GRAPHICS_HPP
```

---

# Professional Convention

Usually:

```text id="0uwmc0"
HEADERNAME_H
```

or

```text id="5uwmc1"
PROJECT_HEADER_H
```

---

# Visual Flow

---

# First Include

```text id="9uwmc2"
F_H not defined
↓
define F_H
↓
include contents
```

---

# Second Include

```text id="3uwmc3"
F_H already defined
↓
skip contents
```

---

# Best Minimal Example

# math.h

```cpp id="7uwmc4"
#ifndef MATH_H
#define MATH_H


int square(int x);


#endif
```

---

# main.cpp

```cpp id="1uwmc5"
#include "math.h"

#include "math.h"


int main()
{
}
```

---

# Without Guard

Compiler sees:

```cpp id="4uwmc6"
int square(int);

int square(int);
```

maybe okay for declarations,
BUT classes/definitions often fail.

---

# Include guards prevent duplicate processing.

---

# Important Note

`#define` also used for macros:

```cpp id="8uwmc7"
#define PI 3.14159
```

But modern C++ prefers:

```cpp id="2uwmc8"
constexpr double pi = 3.14159;
```

instead.

---

# Modern Alternative

Today many compilers support:

```cpp id="6uwmc9"
#pragma once
```

---

# Meaning

```text id="0uwmd0"
include this file only once
```

---

# Simpler Than Guards

Example:

```cpp id="5uwmd1"
#pragma once

int square(int);
```

---

# But

`#ifndef` guards are:

* older
* portable
* standard
* universally supported

So still extremely common.

---

# Most Important Lessons

| Directive     | Meaning                     |
| ------------- | --------------------------- |
| `#ifndef`     | if not defined              |
| `#define`     | create macro/symbol         |
| `#endif`      | end condition               |
| include guard | prevents multiple inclusion |

---

# Real-world Importance

Without include guards:
large C++ projects would constantly suffer:

* redefinition errors
* duplicate declarations
* compile chaos

This tiny pattern became one of the most important patterns in C/C++ history.
------------
# `module Tokenstream;` — C++20 Modules

இது **C++20-இல் அறிமுகப்படுத்தப்பட்ட புதிய தொழில்நுட்பம்** — "Named Modules".

---

## பழைய முறை vs புதிய முறை

**பழைய முறை (Header files):**
```cpp
#include "tokenstream.h"  // முழு header-ஐயும் copy-paste செய்யும்
```

**புதிய முறை (Modules):**
```cpp
module Tokenstream;  // இந்த module-ஐ பயன்படுத்து
```

---

## இந்த கோப்பில் முக்கியமான விஷயங்கள்

### 1. `module Tokenstream;`
இந்த `.cpp` கோப்பு `Tokenstream` என்ற module-ஐ **சேர்ந்தது** என்று அர்த்தம்.

### 2. `export class Token` / `export class Token_stream`
`export` keyword — இந்த class-களை **வெளியே பயன்படுத்த அனுமதிக்கிறது**.

Export இல்லாதது வெளியே தெரியாது — **தானாகவே private**.

### 3. `import std;`
C++ Standard Library முழுவதையும் ஒரே வரியில் இறக்குமதி செய்கிறது. பழைய முறையில்:
```cpp
#include <iostream>
#include <stdexcept>
// ... பல வரிகள்
```

---

## Modules-இன் நன்மைகள்

| பழைய Headers | புதிய Modules |
|---|---|
| Compile time அதிகம் | Compile time குறைவு |
| Macro leakage சாத்தியம் | Macro isolation |
| Include order முக்கியம் | Order முக்கியமில்லை |
| ODR violations சாத்தியம் | பாதுகாப்பானது |

---

சுருக்கமாக சொன்னால், `module Tokenstream;` என்பது இந்த கோப்பை ஒரு **சுயாதீனமான, மறுபயன்படுத்தக்கூடிய அலகாக** (reusable unit) மாற்றுகிறது.

--------------

`அப்படி என்றால் தனியாக ஒன்றை உருவாக்க வேண்டுமா `Tokenstream`? இது உள்ள என்ன இருக்க வேண்டும்?`

# `import std;` vs `#include <iostream>`

---

## அடிப்படை வித்தியாசம்

### `#include <iostream>` — பழைய முறை
```cpp
#include <iostream>
#include <string>
#include <vector>
#include <stdexcept>
// ... தேவையான ஒவ்வொன்றையும் தனியாக சேர்க்க வேண்டும்
```
**Preprocessor** இதை பார்க்கும்போது — அந்த header கோப்பின் **முழு உள்ளடக்கத்தையும் copy-paste** செய்துவிடும்.

### `import std;` — புதிய முறை
```cpp
import std;  // Standard Library முழுவதும் ஒரே வரியில்!
```
Compiler இதை **ஒரு முறை மட்டும் compile செய்து** cache வைத்துக்கொள்ளும்.

---

## விரிவான வித்தியாசங்கள்

| | `#include` | `import std;` |
|---|---|---|
| **வேலை செய்வது** | Copy-paste | Pre-compiled cache |
| **Compile time** | ஒவ்வொரு முறையும் மீண்டும் | ஒரே ஒரு முறை |
| **Macro பரவுமா?** | ஆம் — எங்கும் பரவும் | இல்லை — isolated |
| **என்னென்ன வேண்டும்?** | தனித்தனியாக சேர்க்கணும் | எல்லாமே வரும் |

---

## Macro பிரச்சனை உதாரணம்

```cpp
// பழைய முறையில் ஆபத்து:
#define max 999    // உங்கள் macro

#include <algorithm>  // இதிலும் max இருக்கு!
// 💥 conflict — எது max என்று தெரியாது
```

```cpp
// புதிய முறையில் பாதுகாப்பு:
import std;

#define max 999   // உங்கள் macro — std-ஐ பாதிக்காது ✅
```

---

## Compile Time வித்தியாசம்

```
#include முறை:
main.cpp     → iostream copy → compile → 50,000 வரிகள் process
utils.cpp    → iostream copy → compile → மீண்டும் 50,000 வரிகள்!
helper.cpp   → iostream copy → compile → மீண்டும் மீண்டும்...

import std; முறை:
std module   → ஒரே ஒரு முறை compile → cache ✅
main.cpp     → cache பயன்படுத்து → வேகம்! ⚡
utils.cpp    → cache பயன்படுத்து → வேகம்! ⚡
```

---

## சுருக்கம்

> `#include` என்பது **ஒட்டுமொத்த நூலகத்தையும் உங்கள் வீட்டுக்கு கொண்டு வருவது** — ஒவ்வொரு முறையும்.
>
> `import std;` என்பது **நூலகத்தில் membership வைத்திருப்பது** — தேவையானதை எடுத்துக்கொள்வது, வேகமாக.