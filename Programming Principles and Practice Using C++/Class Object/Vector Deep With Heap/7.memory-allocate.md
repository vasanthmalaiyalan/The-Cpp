```cpp
#include <iostream>
#include <cstdlib>
#include <new>
#include <string>



// ==========================================================
// PERSON CLASS
// ==========================================================

class Person
{
public:

    std::string name;

    Person()
    {
        std::cout
            << "\n[CONSTRUCTOR CALLED]\n";

        name = "Tony Stark";

        std::cout
            << "object address : "
            << this
            << '\n';

        std::cout
            << "name initialized : "
            << name
            << '\n';
    }



    ~Person()
    {
        std::cout
            << "\n[DESTRUCTOR CALLED]\n";

        std::cout
            << "destroying object at : "
            << this
            << '\n';
    }
};



// ==========================================================
// MAIN
// ==========================================================

int main()
{
    std::cout
        << "====================================\n";

    std::cout
        << "STEP 1 : RAW MEMORY ONLY\n";

    std::cout
        << "====================================\n";



    // --------------------------------------------------
    // RAW MEMORY ALLOCATION
    // --------------------------------------------------

    void* raw_memory =
        std::malloc(sizeof(Person));



    std::cout
        << "\nraw memory address : "
        << raw_memory
        << '\n';



    // IMPORTANT:
    // NO CONSTRUCTOR CALLED YET
    // THIS IS NOT A REAL PERSON OBJECT YET



    std::cout
        << "\nNO OBJECT EXISTS YET\n";



    // ==================================================
    // STEP 2
    // CONSTRUCT OBJECT INSIDE MEMORY
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "STEP 2 : PLACEMENT NEW\n";

    std::cout
        << "====================================\n";



    Person* person_ptr =
        new(raw_memory) Person();



    std::cout
        << "\nperson_ptr : "
        << person_ptr
        << '\n';



    std::cout
        << "person name : "
        << person_ptr->name
        << '\n';



    // ==================================================
    // STEP 3
    // USE OBJECT
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "STEP 3 : USING OBJECT\n";

    std::cout
        << "====================================\n";



    person_ptr->name =
        "Ironman";



    std::cout
        << "\nmodified name : "
        << person_ptr->name
        << '\n';



    // ==================================================
    // STEP 4
    // MANUAL DESTRUCTOR
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "STEP 4 : MANUAL DESTRUCTOR\n";

    std::cout
        << "====================================\n";



    person_ptr->~Person();



    std::cout
        << "\nOBJECT DESTROYED\n";



    // IMPORTANT:
    // MEMORY STILL EXISTS
    // ONLY OBJECT LIFETIME ENDED



    // ==================================================
    // STEP 5
    // FREE RAW MEMORY
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "STEP 5 : FREE RAW MEMORY\n";

    std::cout
        << "====================================\n";



    std::free(raw_memory);



    std::cout
        << "\nRAW MEMORY FREED\n";
}
```

---

# இந்த program என்ன கற்பிக்கும்?

## Step 1

```cpp
std::malloc(sizeof(Person))
```

↓

```text
raw bytes மட்டும்
```

🔥 object இல்லை.

---

## Step 2

```cpp
new(raw_memory) Person()
```

↓

```text
constructor runs
real object born
```

🔥

---

## Step 3

```cpp
person_ptr->name
```

↓

இப்போது safe.

ஏனெனில் actual object உருவாகிவிட்டது.

---

## Step 4

```cpp
person_ptr->~Person();
```

↓

```text
object lifetime ends
```

🔥 memory இன்னும் இருக்கும்.

---

## Step 5

```cpp
free(raw_memory);
```

↓

```text
raw storage released
```

🔥

---

# மிக முக்கியமான concept

இந்த program clearly காட்டுவது:

| Thing               | Separate? |
| ------------------- | --------- |
| raw memory          | yes       |
| object construction | yes       |
| object destruction  | yes       |
| memory release      | yes       |

🔥 இதுதான் advanced C++ memory model.
--------------

``void* raw =
    operator new(sizeof(Person));

Person* p =
    new(raw) Person();`இப்படி இந்த மெமரி உள்ளே constructor call செய்ய வைத்தால் மட்டும் என்ன நடக்கும்? `

    அப்படிச் செய்தவுடன் 🔥
அந்த raw memory:

```text id="m7k2x5"
real Person object memory
```

ஆக மாறிவிடும்.

இதுதான் object construction.

---

# Before constructor

இந்த line வரை மட்டும்:

```cpp id="u4m8p1"
void* raw =
    operator new(sizeof(Person));
```

---

Suppose:

```text id="q1k7v4"
raw = 0x5000
```

---

Memory state:

```text id="r9m3x6"
0x5000 -> raw uninitialized bytes
```

🔥

இன்னும்:

```text id="t5k1m8"
Person object இல்லை
```

---

# Important

இந்த நேரத்தில்:

```cpp id="v2m9x4"
Person* p = (Person*)raw;
```

என்று cast செய்தாலும்:

```text id="c7k3p1"
real object இன்னும் உருவாகவில்லை
```

🔥

ஏனெனில் constructor run ஆகவில்லை.

---

# Then this line

```cpp id="n4m8v5"
new(raw) Person();
```

🔥

இதுதான் magic step.

---

# What happens now?

Compiler:

```text id="u1k7x9"
0x5000 memory-ல்
Person constructor run செய்கிறது
```

---

# Suppose class

```cpp id="q5m2v8"
class Person
{
public:

    std::string name;

    Person()
    {
        name = "Tony";
    }
};
```

---

# Constructor effects

இந்த line:

```cpp id="x8k1p4"
name = "Tony";
```

run ஆகும்.

---

# After constructor

Memory:

```text id="r2m7v5"
0x5000 now contains:
- valid std::string
- initialized fields
- real Person object
```

🔥

---

# Most important truth

இந்த moment-ல்தான்:

```text id="t4k8x1"
object lifetime START
```

ஆகிறது.

---

# Before constructor

```text id="u7m3p9"
bytes மட்டும்
```

---

# After constructor

```text id="p1k9v6"
typed living object
```

🔥

---

# Then this becomes legal

```cpp id="n5m4x8"
Person* p =
    (Person*)raw;

std::cout << p->name;
```

🔥

ஏனெனில் actual object exists now.

---

# Before constructor this is UB

```cpp id="q3k7v1"
std::cout << p->name;
```

❌ dangerous.

ஏனெனில்:

```text id="r8m1v4"
std::string object not initialized
```

---

# Deep C++ truth

இந்த:

```cpp id="u6k2x9"
constructor
```

ஒரு initialization function மட்டும் இல்லை.

🔥

It officially:

```text id="c4m9p5"
starts object lifetime
```

---

# Visual timeline

# Step 1

```cpp id="v1k7m3"
operator new(sizeof(Person))
```

↓

```text id="f8m2x6"
raw bytes only
```

---

# Step 2

```cpp id="y5k1v9"
new(raw) Person()
```

↓

```text id="u3m8k4"
constructor runs
object becomes alive
```

🔥

---

# Analogy

## Raw memory

```text id="q7k2v5"
Empty building space
```

---

# Placement new

```text id="r1m9x7"
Build actual house
```

---

# After construction

```text id="t5k3m1"
Now humans can live there
```

🔥

---

# Another important thing

Placement new does NOT:

* allocate new heap
* move memory
* copy bytes

🔥

It only:

```text id="n8m2v4"
runs constructor at specific address
```

---

# Real vector uses this

std::vector internally:

## allocate big raw memory

```text id="u4k7x6"
space for 100 Ts
```

---

## construct only needed objects

```cpp id="p9m1v5"
new(memory_slot) T(...)
```

🔥

---

# Final essence

இந்த:

```cpp id="q2k8m7"
new(raw) Person()
```

செய்தவுடன்:

```text id="r6m3v1"
raw memory bytes
```

↓

```text id="t9k1x4"
real initialized Person object
```

ஆக மாறும்.

🔥 அதாவது:

* constructor run ஆகும்
* members initialize ஆகும்
* object lifetime start ஆகும்
* memory officially Person object ஆக treat செய்யப்படும்.
