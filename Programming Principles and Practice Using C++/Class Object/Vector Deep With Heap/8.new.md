```cpp id="placement_new_internal_demo"
#include <iostream>
#include <cstdlib>
#include <new>
#include <string>



// ==========================================================
// PERSON
// ==========================================================

class Person
{
public:

    std::string name;



    Person()
    {
        std::cout
            << "\n[Person CONSTRUCTOR]\n";

        std::cout
            << "this pointer : "
            << this
            << '\n';

        std::cout
            << "name member address : "
            << &name
            << '\n';


        name = "Tony Stark";


        std::cout
            << "name value : "
            << name
            << '\n';
    }



    ~Person()
    {
        std::cout
            << "\n[Person DESTRUCTOR]\n";

        std::cout
            << "destroying object at : "
            << this
            << '\n';
    }
};



// ==========================================================
// FAKE PLACEMENT NEW
// ==========================================================

Person* fake_placement_new(void* raw_memory)
{
    std::cout
        << "\n====================================\n";

    std::cout
        << "FAKE PLACEMENT NEW\n";

    std::cout
        << "====================================\n";



    // --------------------------------------------------
    // STEP 1
    // CAST RAW MEMORY
    // --------------------------------------------------

    std::cout
        << "\n[STEP 1]\n";

    std::cout
        << "raw memory address : "
        << raw_memory
        << '\n';



    Person* object_ptr =
        (Person*)raw_memory;



    std::cout
        << "casted to Person* : "
        << object_ptr
        << '\n';



    // --------------------------------------------------
    // STEP 2
    // CONSTRUCTOR CALL
    // --------------------------------------------------

    std::cout
        << "\n[STEP 2]\n";

    std::cout
        << "calling constructor manually\n";



    // --------------------------------------------------
    // THIS IS THE IMPORTANT PART
    // --------------------------------------------------

    // compiler internally does something similar
    // when placement new happens

    new(object_ptr) Person();



    // --------------------------------------------------
    // RETURN OBJECT
    // --------------------------------------------------

    return object_ptr;
}



// ==========================================================
// MAIN
// ==========================================================

int main()
{
    std::cout
        << "====================================\n";

    std::cout
        << "STEP 1 : RAW MEMORY\n";

    std::cout
        << "====================================\n";



    // --------------------------------------------------
    // RAW MEMORY ONLY
    // --------------------------------------------------

    void* raw =
        std::malloc(sizeof(Person));



    std::cout
        << "\nraw memory address : "
        << raw
        << '\n';



    std::cout
        << "\nNO OBJECT YET\n";



    // ==================================================
    // CONSTRUCT OBJECT INSIDE MEMORY
    // ==================================================

    Person* person =
        fake_placement_new(raw);



    // ==================================================
    // VERIFY
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "VERIFY OBJECT\n";

    std::cout
        << "====================================\n";



    std::cout
        << "\nperson pointer : "
        << person
        << '\n';



    std::cout
        << "person->name : "
        << person->name
        << '\n';



    std::cout
        << "address of name member : "
        << &person->name
        << '\n';



    // ==================================================
    // MANUAL DESTRUCTION
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "MANUAL DESTRUCTOR\n";

    std::cout
        << "====================================\n";



    person->~Person();



    // ==================================================
    // FREE RAW MEMORY
    // ==================================================

    std::cout
        << "\n\n====================================\n";

    std::cout
        << "FREE RAW MEMORY\n";

    std::cout
        << "====================================\n";



    std::free(raw);
}
```

---

# இப்போது முக்கிய explanation 🔥

இந்த line:

```cpp id="eq1"
new(object_ptr) Person();
```

இதுதான் placement new.

---

# இது என்ன செய்கிறது?

## Before this line

```text id="eq2"
raw memory மட்டும் இருக்கு
```

Suppose:

```text id="eq3"
0x5000
```

---

# object_ptr

```cpp id="eq4"
Person* object_ptr =
    (Person*)raw_memory;
```

↓

```text id="eq5"
0x5000-ஐ
Person object இருக்கும் இடம் போல treat செய்
```

🔥

---

# THEN THIS LINE

```cpp id="eq6"
new(object_ptr) Person();
```

Compiler internally almost:

```text id="eq7"
"0x5000 address-ல்
Person constructor ஓட்டு"
```

🔥

---

# Inside constructor

```cpp id="eq8"
this
```

pointer becomes:

```text id="eq9"
0x5000
```

🔥🔥🔥

---

# Therefore

இந்த:

```cpp id="eq10"
name
```

member actually becomes:

```text id="eq11"
memory inside 0x5000 block
```

---

# This line

```cpp id="eq12"
name = "Tony Stark";
```

↓

```text id="eq13"
0x5000 memory-க்குள் இருக்கும்
std::string object initialize ஆகிறது
```

🔥

---

# Most important truth

Placement new:

```text id="eq14"
memory allocate செய்யாது
```

It only:

```text id="eq15"
runs constructor at exact address
```

🔥

---

# Real internal idea

Conceptually almost:

```cpp id="eq16"
Person::Person(this = 0x5000)
```

🔥

---

# Final deep understanding

இந்த:

```cpp id="eq17"
new(raw) Person();
```

செய்யும்போது:

| Part       | Job                                                      |
| ---------- | -------------------------------------------------------- |
| `raw`      | object எங்கே வாழ வேண்டும்                                |
| `Person()` | எப்படி initialize ஆக வேண்டும்                            |
| `new(...)` | constructor-ஐ அந்த memory location-ோடு connect செய்கிறது |

🔥

அதனால் தான்:

```text id="eq18"
raw bytes
```

↓

```text id="eq19"
living initialized Person object
```

ஆக மாறுகிறது.
